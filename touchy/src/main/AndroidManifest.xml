<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <provider
            android:name="fun.observe.touchy.StartUpDetector"
            android:authorities="${applicationId}.start-up.detector"
            android:exported="false" />

    </application>

</manifest>
